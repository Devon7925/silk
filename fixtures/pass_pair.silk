(export wasm) construct_range := (max: i32) => {
    min = 0,
    max = max,
};

(export wasm) sum_range := (range: {min = i32, max = i32}) => ( 
    mut x := range.min;
    mut res := 0;
    while x <= range.max do (
        res = res + x;
        x = x + 1;
    );
    res
);
